# Archived signals

Signals that were built and backtested but did **not** improve the strategy, kept
for reference. This directory is outside the plugin-discovery glob (`signals/*.py`,
non-recursive), so these are not instantiated on screen/backtest runs and don't
clutter `list-plugins`. To revive one, move it back up into `signals/`.
